/**
 * JPA entities ({@code @Entity}) mapped to database tables. Never serialized directly in
 * HTTP responses — always translated to a DTO via {@code mapper}.
 */
package com.acc.connectauto.entity;
