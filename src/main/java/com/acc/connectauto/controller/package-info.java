/**
 * REST controllers ({@code @RestController}). Receive HTTP requests, delegate to
 * {@code service}, and return DTOs from {@code dto.response}. Never expose {@code entity}
 * classes directly.
 */
package com.acc.connectauto.controller;
