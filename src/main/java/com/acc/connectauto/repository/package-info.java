/**
 * Spring Data JPA repositories. Interfaces extending {@code JpaRepository<Entity, ID>};
 * avoid manual implementations unless a custom query truly requires one.
 */
package com.acc.connectauto.repository;
